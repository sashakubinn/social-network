import { Metadata } from 'next'
import { FC } from 'react'

export const metadata: Metadata = {
	title: 'Settings',
	description: 'Thats page about your settings',
}

const SettingsPage: FC = () => {
	return <div>SettingsPage</div>
}

export default SettingsPage
